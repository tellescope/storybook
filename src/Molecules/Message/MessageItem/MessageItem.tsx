import { Box } from "@mui/material";
import { MessageText } from "./components";

export const MessageItem = () => {
  return (
    <Box>
      <MessageText>Healthy dumpling recipes!</MessageText>
    </Box>
  );
};
